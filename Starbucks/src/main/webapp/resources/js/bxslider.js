/**
 * 
 */
$(document).ready(function(){
		//$('.bxslider').bxSlider();
		$('.bxslider').bxSlider({ 
			auto: true, // 자동 슬라이드 
			autoControls: false, // 시작, 정지버튼 노출 여부
			controls: true, // prev/next 버튼 노출 여부
			pager: true, // 블릿버튼 
			});
		});